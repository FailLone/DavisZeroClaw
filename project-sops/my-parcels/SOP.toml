[sop]
name = "my-parcels"
description = "Check recent parcels through the Davis localhost express proxy."
version = "1.0.0"
priority = "normal"
execution_mode = "supervised"
max_concurrent = 1

[[triggers]]
type = "manual"
